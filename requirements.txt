# requirements.txt
# ============ Core & Web Framework ============
streamlit==1.28.1
streamlit-option-menu==0.3.6

# ============ Data Processing & ML ============
pandas==2.0.3
numpy==1.24.3
scipy==1.11.1
scikit-learn==1.3.0
joblib==1.3.1

# ============ Audio Processing ============
librosa==0.10.0
# Note: sounddevice requires PortAudio (handled in packages.txt)
# Making it non-blocking with try-except in code

# ============ Visualization ============
matplotlib==3.7.2
plotly==5.15.0
pillow==10.0.0

# ============ Database & ORM ============
sqlalchemy>=2.0.0,<3.0.0

# ============ Authentication & Security ============
bcrypt==4.0.1
python-jose==3.3.0
passlib==1.7.4

# ============ Utilities ============
python-multipart==0.0.6
python-dateutil==2.8.2

# ============ ML Model (Lightweight Alternative) ============
# Use scikit-learn instead of heavy TensorFlow for Streamlit Cloud
# If you need TensorFlow later, uncomment and test locally:
# tensorflow==2.13.0
# Note: Keras is included in TensorFlow, don't install separately